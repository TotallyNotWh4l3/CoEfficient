const WeatherChart = () => {
    return (
        <section className="weather-chart">
            <div className="chart-placeholder">Weather Chart (Coming Soon)</div>
        </section>
    );
};

export default WeatherChart;
